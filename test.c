#include <stdio.h>
int main(){
	int number[10] = {0,};//수식이 들어갈 배열
	char ch[10] = {0,};
	int i;
	double result;
	for(i=0;i<3;i++){
		scanf("%d%c"number[i],ch[i]);
	}
	scanf("%d",number[i+1]);
	for(i < 0;i < 10;i++){
		switch(ch[i]){
			case '*':
				result = 
		}
	}
	 printf("%lf",result);
}
